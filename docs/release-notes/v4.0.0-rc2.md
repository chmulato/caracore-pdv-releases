# PREVIEW — not store download

CaraCore PDV 4.0.0-rc2 — prévia Qute.

**Não é o download da loja.** O canal maduro / Free do balcão é `v3.2.3-free`.
Não use a pré-release do topo da lista como se fosse o Free.

Não misture pasta nem data. Canais independentes.
Não apague o AppData da v3 “pra limpar” a v4 — este RC2 **não** abre `%APPDATA%\caracore\data\banco.db`.

O launcher abre o **Edge em modo aplicativo** (não é o Free no navegador).

Loja (Free): https://pdv.caracore.com.br/download.html

## Instalação limpa (obrigatória)

1. Extraia o ZIP em uma pasta **nova** (não por cima do RC1 nem da 3.2.3-free).
2. Não copie a pasta `data` de outro teste.
3. Windows: `iniciar_pdv.bat` (underscore). Linux/macOS: `./iniciar_pdv.sh`.
4. Login: `admin` / `admin` (troca de senha obrigatória).

O banco deste RC fica só em `./data/caracore-pdv.db`.

## Correções em relação à RC1

- Login `admin/admin` no banco isolado deste pacote.
- Launcher honra `./data/caracore-pdv.db` e não reaproveita o SQLite antigo de `%APPDATA%`.
- Logout web em `/logout`.

## Artefato

- `caracore-pdv-4.0.0-rc2-qute-portable.zip`
- SHA256: `5e5d55b6d376c7f1d6ce91a9fb9a73f6d606289af27b508adf2d9eb2cdcd955d`
- Requisitos: Java 25+ e Python 3

A linha Rust (`v0.1.2`, MSI/EXE) é outro produto — não é o Free desta loja.
